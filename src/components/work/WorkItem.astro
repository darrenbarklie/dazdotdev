---
export interface Props {
  content: {
    id: string;
    client: string;
    date: string;
    location: string;
    tech: string[];
    roles: string[];
    urls: URL[] | string;
    blurb: string;
  };
}

const {
  content: { id, client, date, location, tech, roles, urls, blurb },
} = Astro.props as Props;
---

<li id={`work-${id}`} class="border border-gray-400 rounded p-4 pb-6 mb-4">
  <header
    class="flex justify-between items-center border-b border-gray-300 pb-2 mb-4"
  >
    <h3 class="text-xl weight-bold">{client}</h3>
    <span class="text-gray-400">[#{id}]</span>
  </header>

  <div class="flex justify-between mb-2">
    <div>
      <span class="mr-2">{date}</span>|<span class="ml-2">{location}</span>
    </div>

    <div>
      {
        Object.values(roles).map((item) => {
          return <span class="pr-2">{item}</span>;
        })
      }
    </div>
  </div>

  <div class="mt-2 mb-2">
    {
      Object.values(urls).map((item) => {
        return (
          <>
            {/* prettier-ignore */}
            <a href={item} rel="noopener noreferrer" target="_blank" class="pr-4 underline">{item}</a>
          </>
        );
      })
    }
  </div>

  <div id="tech" class="mt-2 mb-2">
    {
      Object.values(tech).map((item) => {
        return (
          <>
            {/* prettier-ignore */}
            <span class="bg-gray-600 text-white text-xs text-small rounded px-2 py-1 mr-1">{item}</span>
          </>
        );
      })
    }
  </div>

  <p>{blurb}</p>
</li>

---
layout: "../../layouts/LayoutBlog.astro"
title: "Getting Started with SurrealDB"
description: "X"
date: "2022-09-29"
categories:
  - Database
  - Learning
  - SurrealDB
---

## Preamble

X

## Table of Contents

## Statements

Statements are used for inserting, updating and deleting data, as well as operating on graph data and general database configuration.
- `USE` : Change NAMESPACE or DATABASE in scope
- `LET` : Import a SurrealQL script file into a local or remote SurrealDB instance
- `BEGIN` : Import a SurrealQL script file into a local or remote SurrealDB instance 
- `CANCEL` : Import a SurrealQL script file into a local or remote SurrealDB instance
- `COMMIT` : Import a SurrealQL script file into a local or remote SurrealDB instance
- IF ELSE : Import a SurrealQL script file into a local or remote SurrealDB instance
- SELECT
- INSERT
- CREATE
- UPDATE
- RELATE
- DELETE
- DEFINE
- REMOVE
- INFO


